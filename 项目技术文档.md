# DST Server Panel — 项目技术文档

> 基于 [Mermaid 流程图语法](https://blog.csdn.net/qq_45324272/article/details/159680814) 与 [Mermaid 高级语法](https://blog.csdn.net/qq_45324272/article/details/159682198) 编写，所有图表使用 Mermaid 渲染。

---

## 一、项目概述

**DST Server Panel** 是一个《饥荒联机版》（Don't Starve Together）专用服务器的 Web 管理面板，部署在 Linux 服务器上，通过浏览器完成服务器的启动/停止、世界编辑、模组管理、控制台指令、玩家管理等全部操作。

| 项目信息 | 说明 |
|----------|------|
| 游戏AppID | `343050`（DST 专用服务器） |
| 操作系统 | Debian GNU/Linux 13 (trixie) x86_64 |
| 运行用户 | `steam` (UID 1000) |
| 面板端口 | `127.0.0.1:5323`（仅回环，通过 nginx 反代对外） |
| 公网入口 | nginx → `/dst/` → `127.0.0.1:5323`（端口 80） |

---

## 二、技术栈

### 后端

| 技术 | 说明 |
|------|------|
| **运行时** | [Bun](https://bun.sh/) v1.3.14（高性能 JS/TS 运行时） |
| **语言** | TypeScript（`"type": "module"`） |
| **HTTP** | `Bun.serve()` 内置 HTTP 服务器 |
| **文件系统** | `node:fs`、`node:path` 原生模块 |
| **加密** | `node:crypto`（SHA-256 HMAC 签名） |
| **进程控制** | `screen` 会话注入 + `Bun.spawn()` |
| **外部工具** | `steamcmd`（模组下载/服务器更新）、`curl`（CDN 下载）、`unzip`（解压） |
| **依赖** | **零 npm 依赖**，仅使用 Bun 内置 + Node 兼容模块 |

### 前端

| 技术 | 说明 |
|------|------|
| **框架** | 原生 JavaScript（无框架，无构建步骤） |
| **样式** | 原生 CSS，CSS 变量主题系统（3 套主题：暗夜琥珀 / 纸质 / 科技） |
| **交互** | `fetch()` 调用 JSON API，DOM 直接操作 |
| **图表** | Mermaid（文档渲染） |

### 基础设施

| 组件 | 说明 |
|------|------|
| **nginx** | 反向代理面板（`/dst/` → `5323`）+ SteamCMD HTTP→HTTPS 代理 |
| **systemd** | `dst-panel.service`（面板进程）、`dst-steam-guard.timer`（权限看门狗） |
| **screen** | 两个命名会话：`dst_master`（地上）、`dst_caves`（地下） |

---

## 三、目录结构

```
/home/steam/
├── steamcmd/                           # SteamCMD 安装目录
├── dst_server/                         # DST 专用服务器
│   ├── bin64/                          # 64位服务器二进制文件
│   ├── mods/                           # 模组目录
│   │   ├── dedicated_server_mods_setup.lua   # 模组下载清单
│   │   └── workshop-<id>/             # 已安装的模组
│   ├── ugc_mods/                       # 按存档分片的模组软链
│   └── version.txt                     # 服务器版本号
├── .klei/DoNotStarveTogether/          # 存档数据根目录
│   ├── MyDediServer/                   # 存档 1（原版）
│   │   ├── cluster.ini                 # 存档配置
│   │   ├── cluster_token.txt           # Klei 服务器令牌
│   │   ├── adminlist.txt               # 管理员列表
│   │   ├── blocklist.txt               # 黑名单
│   │   ├── Master/                     # 地上世界分片
│   │   │   ├── server.ini              # 分片配置（端口等）
│   │   │   ├── modoverrides.lua        # 模组启用与配置
│   │   │   ├── leveldataoverride.lua   # 世界生成设置
│   │   │   ├── server_log.txt          # 服务器日志
│   │   │   ├── server_chat_log.txt     # 聊天日志
│   │   │   └── save/session/<id>/      # 存档快照（按天数）
│   │   └── Caves/                      # 地下世界分片（结构同 Master）
│   └── Shipwrecked_Together/           # 存档 2（海难模组）
├── dst_panel/                          # Web 管理面板
│   ├── src/
│   │   ├── server.ts                   # 后端主文件（2400+ 行）
│   │   ├── items.ts                    # 全量物品表（2370 条）
│   │   └── worldgen.ts                 # 世界生成选项表
│   ├── public/
│   │   ├── index.html                  # 主界面
│   │   ├── login.html                  # 登录页
│   │   ├── app.js                      # 前端逻辑（2000+ 行）
│   │   └── style.css                   # 样式（CSS 变量主题）
│   ├── panel_config.json               # 运行时配置
│   ├── .panel_password                 # 登录密码
│   ├── .panel_salt                     # 认证盐值
│   └── mod_cache.json                  # Steam Workshop API 缓存
├── start_dst.sh                        # 启动服务器脚本
├── stop_dst.sh                         # 停止服务器脚本
└── update_dst.sh                       # 更新服务器脚本
```

---

## 四、系统架构图

```mermaid
flowchart TB
    subgraph 用户["用户层"]
        U["🌐 浏览器"]
    end

    subgraph 代理["反向代理层"]
        N["nginx :80<br/>/dst/ → :5323"]
    end

    subgraph 面板["面板层 (Bun)"]
        P["dst_panel :5323<br/>TypeScript 单文件后端"]
        P --> AUTH["认证模块<br/>SHA-256 挑战响应"]
        P --> API["API 路由<br/>53 个接口"]
        P --> CRON["定时任务<br/>自动重启 / 自动公告"]
    end

    subgraph 游戏服务器["游戏服务器层"]
        SM["screen: dst_master<br/>地上世界 (Master)"]
        SC["screen: dst_caves<br/>地下世界 (Caves)"]
    end

    subgraph 数据层["数据层"]
        KLEI["📁 .klei/DoNotStarveTogether/<br/>存档配置/日志/存档快照"]
        MODS["📁 dst_server/mods/<br/>模组文件"]
        CFG["📁 panel_config.json<br/>面板运行时配置"]
        CACHE["📁 mod_cache.json<br/>Steam API 缓存 (6h TTL)"]
    end

    subgraph 外部["外部服务"]
        STEAM["Steam Workshop API<br/>模组信息/下载"]
        STEAMCMD["SteamCMD<br/>模组/服务器更新"]
    end

    U -->|"HTTP 请求"| N
    N -->|"反向代理"| P
    P -->|"screen -X stuff<br/>Lua 注入"| SM
    P -->|"screen -X stuff<br/>Lua 注入"| SC
    P -->|"读写文件"| KLEI
    P -->|"读写模组"| MODS
    P -->|"读取/保存"| CFG
    P -->|"缓存/读取"| CACHE
    P -->|"查询模组信息"| STEAM
    P -->|"下载模组"| STEAMCMD
    SM -.->|"内部通信<br/>:10889"| SC
```

---

## 五、认证流程

```mermaid
sequenceDiagram
    participant U as 浏览器
    participant S as 面板后端

    U->>S: GET /api/login/challenge
    S-->>U: { salt, nonce }（nonce 60秒有效）

    Note over U: 计算 hash = SHA256(<br/>SHA256(salt + password) + nonce)

    U->>S: POST /api/login { hash, nonce }
    Note over S: 用 timingSafeEqual<br/>验证 hash
    alt 验证成功
        S-->>U: Set-Cookie: dstp_session=<id>.<HMAC签名><br/>{ ok: true }
    else 验证失败
        S-->>U: { ok: false, msg: "密码错误" }
    end

    Note over U,S: 后续所有 /api/* 请求<br/>自动携带 Cookie
    U->>S: GET /api/basic (Cookie: dstp_session=...)
    Note over S: checkAuth() 验证签名
```

---

## 六、API 路由结构

```mermaid
flowchart LR
    subgraph 认证["认证"]
        L1["GET /login/challenge"]
        L2["POST /login"]
        L3["POST /logout"]
    end

    subgraph 基本设置["基本设置"]
        B1["GET/POST /basic<br/>存档配置/令牌/路径"]
        B2["POST /cluster<br/>切换存档"]
        B3["POST /cluster/rename<br/>重命名存档"]
        B4["POST /cluster/create<br/>新建存档"]
        B5["POST /cluster/delete<br/>删除存档"]
    end

    subgraph 世界编辑["世界编辑"]
        W1["GET /worlds<br/>分片列表"]
        W2["POST /worlds/add<br/>添加分片"]
        W3["POST /worlds/delete<br/>删除分片"]
        W4["GET/POST /world/overrides<br/>世界生成设置"]
        W5["GET /world/modworldgen<br/>模组世界设置"]
    end

    subgraph 模组管理["模组管理"]
        M1["GET /mods<br/>模组列表"]
        M2["GET /mods/detail<br/>模组详情"]
        M3["POST /mods/save-enabled<br/>启用/禁用"]
        M4["POST /mods/config<br/>保存配置"]
        M5["POST /mods/add<br/>注册模组"]
        M6["POST /mods/delete<br/>取消订阅"]
        M7["GET /mods/search<br/>搜索创意工坊"]
        M8["POST /mods/download<br/>下载模组"]
    end

    subgraph 服务器控制["服务器控制"]
        S1["GET /server/status<br/>运行状态"]
        S2["POST /server/start<br/>启动"]
        S3["POST /server/stop<br/>停止"]
        S4["POST /server/restart<br/>重启"]
        S5["POST /server/pause<br/>暂停/恢复"]
        S6["GET/POST /server/adminlist<br/>管理员列表"]
        S7["GET/POST /server/blocklist<br/>黑名单"]
    end

    subgraph 控制台["控制台"]
        C1["POST /console/exec<br/>执行Lua"]
        C2["POST /console/players<br/>在线玩家"]
        C3["POST /console/worldinfo<br/>世界信息"]
        C4["GET /items<br/>物品表"]
        C5["GET/POST /item-history<br/>物品使用历史"]
        C6["GET /saves/list<br/>存档快照"]
        C7["POST /saves/rollback<br/>回档"]
    end

    subgraph 日志["日志"]
        LG1["GET /serverlog<br/>服务器日志"]
        LG2["GET /chatlog<br/>聊天日志"]
        LG3["GET /playerlog<br/>玩家记录"]
        LG4["GET /logs/list<br/>日志文件列表"]
    end
```

---

## 七、服务器启动流程

```mermaid
flowchart TD
    A["点击「启动服务器」"] --> B{"分片是否<br/>已在运行？"}
    B -->|"是"| Z["提示：已在运行"]
    B -->|"否"| C["读取 panel_config.json<br/>获取 cluster 名称"]
    C --> D{"模式判断"}
    D -->|"online"| E["启动参数：<br/>-cluster <name> -shard <shard>"]
    D -->|"offline"| F["启动参数：<br/>-cluster <name> -shard <shard><br/>-offline"]
    E --> G["screen -dmS dst_master<br/>启动地上世界"]
    F --> G
    G --> H["screen -dmS dst_caves<br/>启动地下世界"]
    H --> I["设置 slotBusy<br/>返回结果"]

    J["定时任务每 30s"] --> K{"autorestart<br/>已开启？"}
    K -->|"否"| L["跳过"]
    K -->|"是"| M{"分片进程<br/>存活？"}
    M -->|"是"| L
    M -->|"否"| N["自动重启<br/>该分片"]
```

---

## 八、模组下载流程

```mermaid
flowchart TD
    A["用户添加模组 ID"] --> B["加入下载队列"]
    B --> C{"有空闲并行槽？<br/>（最多 3 个）"}
    C -->|"否"| D["等待"]
    C -->|"是"| E["开始下载"]
    E --> F{"mod_cache.json<br/>有 CDN 直链？"}
    F -->|"有"| G["curl 下载 ZIP"]
    G --> H{"下载成功？"}
    H -->|"是"| I["unzip 解压到<br/>mods/workshop-<id>"]
    H -->|"否"| J["回退 steamcmd"]
    F -->|"无"| J
    J --> K["steamcmd 下载<br/>（串行，全局锁）"]
    K --> L{"下载成功？"}
    L -->|"是"| M["复制到<br/>mods/workshop-<id>"]
    L -->|"否"| N["❌ 标记失败"]
    I --> O["✅ 标记成功"]
    M --> O
```

---

## 九、控制台命令执行流程

```mermaid
sequenceDiagram
    participant U as 浏览器
    participant P as 面板后端
    participant S as screen 会话
    participant L as server_log.txt

    U->>P: POST /api/console/exec { lua: "c_save()" }
    P->>P: 验证登录 + 服务器运行中

    alt 普通命令（fire-and-forget）
        P->>S: screen -S dst_master -X stuff "c_save()\n"
        S-->>P: exit code 0
        P-->>U: { ok: true, msg: "命令已发送" }
    end

    alt 需要返回值的命令（如获取玩家列表）
        P->>S: screen stuff "print(DSTPANEL_BEGIN:<rand>)"
        Note over P: 等待 500ms
        P->>S: screen stuff "实际 Lua 命令"
        Note over P: 等待 1500ms
        P->>L: 读取 server_log.txt
        Note over P: 从 BEGIN 标记处<br/>截取后续输出
        P->>P: 正则解析<br/>DSTPANEL_PL:KU_xxx:玩家名
        P-->>U: { players: [...] }
    end
```

---

## 十、文件 ER 图（实体关系）

```mermaid
erDiagram
    CLUSTER ||--o{ SHARD : contains
    CLUSTER ||--|| CLUSTER_INI : "cluster.ini"
    CLUSTER ||--|| CLUSTER_TOKEN : "cluster_token.txt"
    CLUSTER ||--|| ADMINLIST : "adminlist.txt"
    CLUSTER ||--|| BLOCKLIST : "blocklist.txt"
    SHARD ||--|| SERVER_INI : "server.ini"
    SHARD ||--|| MODOVERRIDES : "modoverrides.lua"
    SHARD ||--|| LEVELDATAOVERRIDE : "leveldataoverride.lua"
    SHARD ||--|| SERVER_LOG : "server_log.txt"
    SHARD ||--|| CHAT_LOG : "server_chat_log.txt"
    SHARD ||--o{ SAVE_SESSION : "save/session/"
    SAVE_SESSION ||--o{ SAVE_SNAPSHOT : "按天数快照"

    CLUSTER {
        string name PK "存档名 如 MyDediServer"
        string cluster_name "房间名"
        string game_mode "survival/endless/wilderness"
        string mode "online/offline"
        int max_players "最大人数"
    }

    SHARD {
        string name PK "Master 或 Caves"
        boolean is_master "是否主分片"
        string server_port "游戏端口"
        boolean running "运行状态"
    }

    CLUSTER_INI {
        string section "GAMEPLAY/NETWORK/SHARD/MISC"
        string key "配置键"
        string value "配置值"
    }

    CLUSTER_TOKEN {
        string token "pds-g^KU_xxx^..."
    }

    MODOVERRIDES {
        string workshop_id PK "workshop-<id>"
        boolean enabled "是否启用"
        json configuration_options "模组配置"
    }

    ADMINLIST {
        string ku_id PK "KU_xxx"
        string note "备注"
    }

    BLOCKLIST {
        string ku_id PK "KU_xxx"
        string note "备注"
    }

    SAVE_SNAPSHOT {
        int day PK "天数编号 如 0000000002"
        binary data "二进制存档数据"
        string meta "元数据文件"
    }
```

---

## 十一、面板配置文件 ER 图

```mermaid
erDiagram
    PANEL_CONFIG ||--|| CLUSTER_REF : "当前存档"
    PANEL_CONFIG ||--o{ ANNOUNCEMENT : "公告列表"
    PANEL_CONFIG ||--|| ANNOUNCE_AUTO : "自动公告配置"
    PANEL_CONFIG ||--o{ ITEM_HISTORY : "物品使用历史"
    PANEL_CONFIG ||--|| SERVER_DIR_REF : "服务器目录"

    PANEL_CONFIG {
        string cluster "当前存档名"
        boolean beta "是否内测分支"
        string betaBranch "内测分支名"
        string mode "online/offline"
        boolean autorestart "自动重启"
        string serverDir "服务器目录路径"
    }

    ANNOUNCE_AUTO {
        boolean enabled "是否启用"
        int intervalSec "间隔秒数"
        int idx "当前轮转索引"
        int lastSent "上次发送时间戳"
    }

    ITEM_HISTORY {
        string prefab PK "物品 prefab 名"
    }

    ANNOUNCEMENT {
        string text "公告文本"
    }
```

---

## 十二、模组管理架构

```mermaid
flowchart TB
    subgraph 模组来源["模组文件来源（全局唯一存储）"]
        DS["dst_server/mods/workshop-<id><br/>面板下载安装"]
        UGC["dst_server/ugc_mods/<cluster>/<shard>/content/322330/<id><br/>服务器运行时自动下载"]
    end

    subgraph 配置层["启用配置（按存档独立）"]
        SETUP["dedicated_server_mods_setup.lua<br/>ServerModSetup(ID) 注册下载清单"]
        OVR_M["Master/modoverrides.lua<br/>启用状态 + 配置项"]
        OVR_C["Caves/modoverrides.lua<br/>启用状态 + 配置项"]
    end

    subgraph 缓存层["Steam API 缓存"]
        CACHE["mod_cache.json<br/>模组标题/描述/订阅数/更新日期<br/>6小时 TTL"]
    end

    DS --> OVR_M
    DS --> OVR_C
    UGC --> OVR_M
    UGC --> OVR_C
    SETUP -.->|"注册下载"| DS
    CACHE -.->|"元数据来源"| DS
```

---

## 十三、端口映射

| 端口 | 协议 | 用途 | 公网开放 |
|------|------|------|----------|
| 11000 | UDP+TCP | 地上世界游戏端口 | ✅ |
| 11001 | UDP+TCP | 地下世界游戏端口 | ✅ |
| 27018 | UDP | 地上 Steam Master | ✅ |
| 27019 | UDP | 地下 Steam Master | ✅ |
| 8768 | UDP | 地上 Steam 认证 | ✅ |
| 8769 | UDP | 地下 Steam 认证 | ✅ |
| 10889 | TCP | 分片间通信（Master↔Caves） | ❌ 仅回环 |
| 5323 | TCP | 面板 (Bun) | ❌ 仅回环 |
| 80 | TCP | nginx（面板反代 + SteamCMD 代理） | ✅ |

---

## 十四、源码文件结构

```mermaid
flowchart TD
    subgraph 后端["后端 src/"]
        ST["server.ts<br/>主文件 2400+ 行"]
        IT["items.ts<br/>物品表 2370 条"]
        WG["worldgen.ts<br/>世界生成选项"]
    end

    subgraph 前端["前端 public/"]
        IH["index.html<br/>主界面"]
        LH["login.html<br/>登录页"]
        AJ["app.js<br/>前端逻辑 2000+ 行"]
        CSS["style.css<br/>样式 + 主题"]
    end

    subgraph ST内部["server.ts 内部结构"]
        R1["路径常量 & 配置加载"]
        R2["INI 解析/序列化"]
        R3["认证模块 (challenge-response)"]
        R4["进程控制 (screen/启动/停止)"]
        R5["modinfo.lua 解析器"]
        R6["Steam Workshop API + 缓存"]
        R7["模组下载队列 (CDN + steamcmd)"]
        R8["创意工坊搜索 (HTML 抓取)"]
        R9["日志解析"]
        R10["API 路由 (53 个接口)"]
        R11["定时任务 (自动重启/公告)"]
    end

    ST --> R1 & R2 & R3 & R4 & R5 & R6 & R7 & R8 & R9 & R10 & R11
```

---

## 十五、定时任务流程

```mermaid
sequenceDiagram
    participant T as 定时器
    participant P as 面板
    participant S as 服务器

    loop 每 30 秒
        T->>P: 触发自动重启检查
        alt autorestart 已开启
            P->>P: 检查每个分片存活状态
            alt 分片已死
                P->>S: 自动重启该分片
            end
        end
    end

    loop 每 15 秒
        T->>P: 触发自动公告检查
        alt 自动公告已开启 且 到达间隔
            P->>S: c_announce("公告内容")
            Note over P: 轮转 idx，保存配置
        end
    end
```

---

## 十六、用户操作流程（页面导航）

```mermaid
flowchart LR
    L["登录页"] -->|登录成功| M["主面板"]

    M --> T1["基本设置"]
    M --> T2["编辑世界"]
    M --> T3["模组管理"]
    M --> T4["服务器管理"]
    M --> T5["控制台"]
    M --> T6["玩家记录"]
    M --> T7["聊天记录"]
    M --> T8["帮助"]

    T1 --> T1a["存档配置"]
    T1 --> T1b["令牌/路径"]
    T1 --> T1c["存档切换/新建"]

    T2 --> T2a["世界列表"]
    T2 --> T2b["生成设置"]
    T2 --> T2c["存档管理"]

    T3 --> T3a["本地模组"]
    T3 --> T3b["搜索/下载"]

    T4 --> T4a["启动/停止"]
    T4 --> T4b["管理员/黑名单"]
    T4 --> T4c["实时日志"]

    T5 --> T5a["玩家操作"]
    T5 --> T5b["物品发放"]
    T5 --> T5c["全局操作"]
    T5 --> T5d["存档回档"]
    T5 --> T5e["公告管理"]
```

---

## 参考资料

- [Mermaid 流程图语法（一）](https://blog.csdn.net/qq_45324272/article/details/159680814)
- [Mermaid 高级语法（二）](https://blog.csdn.net/qq_45324272/article/details/159682198)
- [Mermaid 官方文档](https://mermaid.js.org/)
- [Mermaid 在线编辑器](https://mermaid.live/)
